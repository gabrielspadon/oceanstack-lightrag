# Project: code — the OceanStack code/schema/architecture knowledge graph.
# This is the original deployment; values mirror the live :9621 server so that
# PROJECT=code reproduces current behavior exactly (backward compatibility).
WORKSPACE=oceanstack_code_schema
PORT=9621
WORKING_DIR=/fast-array/lightrag/rag-storage
INPUT_DIR=/fast-array/lightrag/inputs
POSTGRES_DATABASE=lightrag
LLM_MODEL=gemma4:26b-a4b-it-q4_K_M
EMBEDDING_MODEL=qwen3-embedding:0.6b
EMBEDDING_DIM=1024
